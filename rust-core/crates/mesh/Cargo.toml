[package]
name = "mesh"
version = "0.1.0"
edition = "2021"

[dependencies]
common = { path = "../common" }
identity = { path = "../identity" }
serde = { version = "1.0", features = ["derive"] }
tokio = { version = "1.0", features = ["full"] }
